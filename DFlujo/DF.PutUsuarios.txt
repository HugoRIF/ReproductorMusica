@startuml
title __PUT usuarios__
start

	
if(id: definido \n nombre: definido \n contrasenia: definido \n tipo_usuario: definido) then (si)
:validarParametros(id,nombre,contrasenia,tipo_usuario);
floating note left
Validar Datos de Usuario[]:
Los datos pueden estar vacios solo se modifican los que no esten vacios
el id es el unico que debe estar lleno siempre
 id,
 nombre,
 contrasenia,
 tipo_usuario
regresa los parametros que si existen
end note
	if (Parametros Validos?) then (si)

		:UPDATE usuario SET nombre, contrasenia, tipo_usuario WHERE id= Usuario[id] ;
		 note left: Hacer UPDATE a la tabla 'usuario',\n Solo se modifican los datos llenos
		if (Query exitoso?) then (si)
			:crearRespuesta(200, "Usuario actualizado", []);
		else (no)
			:crearRespuesta(400, "No se puede actualizar", []);
floating note right
crearRespuesta() genera el arreglo Respuesta=[
    codigo
    mensaje
    data[]
]
con los parametros que s ele pasan.
end note
		endif
	else (no)
		:crearRespuesta(200, "No se modifico nada", []);
	endif
	
else (no)
:crearRespuesta(404, "No se encontraron Datos", []);
endif
:mandarRespuesta(Repuesta);
stop
@enduml